# Codex 工具说明

本参考只说明 Codex 内可用工具如何支持工作流；它不授予额外权限，也不替代仓库规则。

## 常用映射

| 目标 | Codex 做法 | 边界 |
| --- | --- | --- |
| 检查仓库状态 | 使用只读 Git 命令与文件读取 | 不因检查而改变仓库状态 |
| 修改文本 | 使用补丁编辑并审查 diff | 仅修改已授权范围 |
| 运行验证 | 使用 PowerShell 执行目标测试或脚本 | 记录命令、结果和未运行项 |
| 维护多交付任务状态 | 使用 `update_plan` 映射书面计划中的语义交付 | 保持恰好一个 `in_progress`，不拆微步骤 |
| 调用子代理 | 对可独立、边界清晰的任务委派 | 主代理负责最终判断与写入 |
| 创建提交或推送 | 先核对明确授权与当前分支 | 提交不等于允许推送 |

## 计划与任务状态

`update_plan` 是 Codex 的**会话内进度**和恢复表。书面计划包含多个有意义交付、预计跨多个工具
调用或对话轮次，或中断恢复会受益时，在修改实现文件前创建任务表。每个条目对应一个语义交付，
不是计划里的每条命令、测试或提交动作；创建和切换时保持**恰好一个** `in_progress`。

任务表**不能替代持久计划**、Git 提交或验证证据。交付只有在相称验证和提交成功后才能标记完成；
模型按证据合并、拆分、重排或替换任务时，应同步 `update_plan` 并说明实质原因。

## Windows 说明

此环境默认使用 PowerShell。读取和写入中文文本时显式使用 UTF-8；命令有顺序依赖时，
拆成独立调用或使用 PowerShell 语法，不要假定 Bash 行为可用。

## 工作树检查

开始可能影响分支的工作前，可用只读 Git 命令确认当前分支和工作树：

```powershell
git branch --show-current
git status --short
git rev-parse --git-dir
git rev-parse --git-common-dir
```

如果已经处在链接工作树中，不要重复创建工作树；若处于分离 HEAD，则在没有明确授权时
不要尝试推送或创建拉取请求。
